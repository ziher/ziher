<%= form_for(@inventory_journal) do |f| %>
  <% if @inventory_journal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_journal.errors.count, "error") %> prohibited this inventory_journal from being saved:</h2>

      <ul>
      <% @inventory_journal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :jednostka %><br />
    <%= f.collection_select :unit_id, Unit.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :open %><br />
    <%= f.check_box :open %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
