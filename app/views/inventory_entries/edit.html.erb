<h1><%= @inventory_entry.is_expense ? "Rozchód" : "Przychód" %> inwentarzowy</h1>

<%= render 'form' %>

    <div class="row top-buffer-20">
      <div class="col-md-12">
        <%= link_to 'Powrót do książki', inventory_entries_path, class: "btn btn-sm btn-default" %>
      </div>
    </div>
