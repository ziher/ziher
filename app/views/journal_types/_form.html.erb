<%= form_for(@journal_type) do |f| %>
  <% if @journal_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@journal_type.errors.count, "error") %> prohibited this journal_type from being saved:</h2>

      <ul>
      <% @journal_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
