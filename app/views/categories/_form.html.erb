<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div><br />
  <div class="field">
    <%= f.label :year %>
    <%= f.text_field :year %>
  </div><br />
  <div class="field">
    <%= f.label :is_expense %>
    <%= f.check_box :is_expense %>
  </div><br />
  <div class="field">
    <%= f.label :is_one_percent %>
    <%= f.check_box :is_one_percent %>
  </div><br />
  <div class="actions">
    <%= f.submit I18n.t(:save)%>
  </div>
<% end %>
